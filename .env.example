# Copy this file to .env and fill in your real values.
# .env is in .gitignore — it will never be committed.

ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: change the port the server listens on (default 3000)
# PORT=3000
